FROM mysql:latest

ENTRYPOINT mysql --protocol=tcp -h $SQL_HOST -u$SQL_ROOT_USER -p$SQL_ROOT_PASSWORD -e "CREATE USER '$SQL_CMS_USER'@'%' IDENTIFIED BY '$SQL_CMS_PASSWORD'; GRANT ALL PRIVILEGES ON $SQL_DATABASE_NAME . * TO '$SQL_CMS_USER'@'%'; FLUSH PRIVILEGES;"